# Training handoff

## Current state

- Candidate: `res10-r4-cp62`, initialized from `res10:62`.
- Latest checkpoint: **20**. Checkpoints 0–20 exist; do not initialize the
  candidate again.
- Replay: 1,527,111 aligned rows, of which 502,111 were generated by this
  experiment.
- Checkpoint 20: persistent Adam step 500, 2,048,000 total training exposures,
  69 optimizer-state entries. All checkpoints reload and have finite parameters.
- Checkpoints 11–20 were produced in ten successful bounded updates with no
  failed training runs. The latest run added 250,713 positions, almost exactly
  the intended ten-trigger budget. Worker and servers are stopped.

Fixed 4,096-example offline results:

| Checkpoint | Policy CE | Value MSE | Sign accuracy | Value bias |
| ---: | ---: | ---: | ---: | ---: |
| 0 | 2.0022 | 0.3132 | 88.09% | +0.0035 |
| 1 | 1.9861 | 0.3033 | 89.01% | -0.0008 |
| 5 | 1.9797 | 0.2998 | 88.72% | -0.0233 |
| 10 | 1.9772 | 0.2953 | 89.14% | -0.0060 |
| 20 | 1.9765 | 0.2948 | 89.09% | -0.0023 |

All 69 checkpoint-20 gradient tensors were finite and non-zero. The earlier
32-position screen of checkpoint 1 versus 0 was neutral: 31–29 with four
draws, score 51.6%, paired 95% CI 41.7%–61.4%.

The full fixed-corpus checkpoint-10 evaluation was positive. Over 128 positions
and both seat assignments (256 games), checkpoint 10 beat checkpoint 0 by
149–104 with three draws: score 58.8%, paired-position 95% CI 53.4%–64.2%.
The two seat results were 99–28 with one draw for checkpoint 10 as P1, then
76–50 with two draws for checkpoint 0 as P1. The raw result is stored in
`nbench-checkpoint10.jsonl` (exactly two JSONL records). Benchmark containers
and ports 50171/50172 were clean after the run.

The full checkpoint-20 evaluation against checkpoint 0 was also positive.
Over 128 positions and both seat assignments, checkpoint 20 beat checkpoint 0
by 143–105 with eight draws: score 57.4%, paired-position 95% CI
52.3%–62.5%. The two seat results were 100–25 with three draws for checkpoint
20 as P1, then 80–43 with five draws for checkpoint 0 as P1. Raw results are
in `nbench-checkpoint20.jsonl`.

A direct checkpoint-20 versus checkpoint-10 match leaned toward checkpoint 20
but was neutral: 136–113 with seven draws, score 54.5%, paired-position 95% CI
49.3%–59.7%. The two seat results were 88–37 with three draws for checkpoint
20 as P1, then 76–48 with four draws for checkpoint 10 as P1. This supports a
positive trend, but does not establish that checkpoints 11–20 improved playing
strength beyond checkpoint 10. Raw results are in
`nbench-checkpoint20-vs10.jsonl`.

## Start here next

Checkpoint 20 remains significantly stronger than checkpoint 0, while the
direct checkpoint-20 versus checkpoint-10 result is neutral. A reasonable next
learning-curve point is checkpoint 30, preserving the replay window and
optimizer state. Evaluate checkpoint 30 directly against checkpoint 20 (and
against checkpoint 0 if an absolute baseline measurement is still useful).
Do not initialize a new candidate or overwrite existing checkpoints.

For reference, the completed checkpoint-20 baseline match was:

```bash
HEXZ_NBENCH_GAMES=128 \
HEXZ_NBENCH_ITERATIONS=800 \
HEXZ_NBENCH_CONCURRENCY=64 \
HEXZ_NBENCH_STATS_FILE=/home/dw/data/hexz-models/models/flagz/res10-r4-cp62/nbench-checkpoint20.jsonl \
bash scripts/nbench2.sh res10-r4-cp62:20 res10-r4-cp62:0
```

To reproduce the offline comparison:

```bash
PYTHONPATH=pyhexz/src pyhexz/.venv/bin/python3 scripts/eval_checkpoints.py \
  --repo /home/dw/data/hexz-models \
  --model res10-r4-cp62 \
  --checkpoints 0,10,20 \
  --examples 4096 \
  --batch-size 1024 \
  --seed 1 \
  --sample-population 1025000 \
  --device cuda
```

Relevant artifacts:

- `/home/dw/data/hexz-models/models/flagz/res10-r4-cp62/`
- `/home/dw/data/hexz-models/models/flagz/res10-r4-cp62/round1-summary.json`
- `/home/dw/data/hexz-models/models/flagz/res10-r4-cp62/nbench-checkpoint10.jsonl`
- `/home/dw/data/hexz-models/models/flagz/res10-r4-cp62/nbench-checkpoint20.jsonl`
- `/home/dw/data/hexz-models/models/flagz/res10-r4-cp62/nbench-checkpoint20-vs10.jsonl`
- `log/training.log` and `log/worker.log`
